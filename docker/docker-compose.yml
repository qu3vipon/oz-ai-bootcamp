services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    depends_on:
      - db
    volumes:
      - ./api:/app

  worker:
    build: ./worker

  db:
    image: mysql:8.4
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: app_db
    ports:
      - "33060:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
